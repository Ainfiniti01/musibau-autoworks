import React from 'react';

const CustomerNavbar = () => {
  return (
    <nav>
      {/* Customer Navbar content: name/avatar, logout button */}
      <div>Customer Name / Avatar</div>
      <button>Logout</button>
    </nav>
  );
};

export default CustomerNavbar;
